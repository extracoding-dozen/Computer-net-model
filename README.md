# Network Graph Model 🌐

**Модель компьютерной сети на языке C с использованием теории графов.**

Этот проект представляет собой симуляцию компьютерной сети, реализованную с нуля на языке C. Программа моделирует сетевые устройства и соединения между ними, позволяя выполнять маршрутизацию данных, анализировать топологию сети и визуализировать её структуру.

---

## 📋 Особенности и Функционал

Проект реализует направленный взвешенный граф (список смежности) и предоставляет интерактивный консольный интерфейс для управления сетью.

### Основные возможности:
*   **CRUD операции:** Добавление, удаление и редактирование вершин (компьютеров) и ребер (соединений).
*   **Маршрутизация (Алгоритм Дейкстры):** Поиск кратчайшего пути передачи данных между двумя устройствами с учетом пропускной способности портов.
*   **Анализ связности:** Разбиение сети на компоненты связности (поиск подсетей).
*   **Поиск доступности:** Определение всех устройств, доступных из заданной точки (обход в ширину/глубину).
*   **Визуализация:** Генерация описания графа на языке **DOT** (Graphviz) для графического отображения топологии.
*   **Работа с файлами:** Импорт конфигурации сети из текстового файла.

## 🛠 Технический стек

*   **Язык:** C (C99/C11)
*   **Сборка:** GNU Make
*   **Структуры данных:** Списки смежности, Очереди.
*   **Библиотеки:** `libreadline` (для удобного ввода в CLI).
*   **Инструменты качества:** AddressSanitizer (ASan) для контроля утечек памяти и ошибок доступа.

## 🏗 Структура данных

Сеть реализована как **список смежности**, что обеспечивает эффективность по памяти для разреженных графов.

```c
// Упрощенная схема структур (из lib/graph.h)
typedef struct Vertex {
    char * name;           // Имя устройства
    uns_i port;            // Порт устройства
    struct Edge * edlist;  // Список исходящих соединений
    // ... служебные поля для алгоритмов
} Vertex;

typedef struct Edge {
    Vertex * to;           // Указатель на целевое устройство
    uns_i * allowed_ports; // Массив разрешенных портов
    // ...
} Edge;
```
    
## 🚀 Сборка и Запуск

Для сборки проекта используется `Makefile`. В проект встроен санитайзер адресов (ASan) для обеспечения надежности кода.

### Предварительные требования
Вам понадобится компилятор `gcc`, `make` и библиотека `libreadline`.

*   **Ubuntu/Debian:**
    ```bash
    sudo apt-get install libreadline-dev
    ```
*   **macOS:**
    ```bash
    brew install readline
    ```

### Инструкции

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/extracoding-dozen/Computer-net-model.git
    ```

2.  **Соберите проект:**
    ```bash
    make build_asan
    ```

3.  **Запустите:**
    ```bash
    ./bin_asan
    ```

4.  **Очистка бинарных файлов:**
    В `Makefile` предусмотрено удаление объектных файлов после сборки, но для полной очистки можно использовать:
    ```bash
    rm bin_asan
    ```

---

## 📖 Использование

После запуска программы появится интерактивное меню. Основные возможности:

*   **Вставка вершины** — добавить новый компьютер.
*   **Вставка ребра** — создать соединение между компьютерами (требуется указать порты).
*   **Вывод графический** — выводит структуру графа в формате DOT (можно визуализировать через [WebGraphviz](http://www.webgraphviz.com/)).
*   **Поиск кратчайшего пути** — введите имена двух компьютеров и порт, программа рассчитает оптимальный маршрут.
*   **Разбить на компоненты связности** — показывает изолированные подсети.

## 🧪 Автоматизация и Тестирование на больших данных

В репозитории предусмотрен Python-скрипт для генерации синтетических данных (стресс-тестирования). Скрипт создает файл `input.txt`, содержащий описание сети из **1000-2000 устройств**, намеренно разбитых на изолированные кластеры (подсети). Это позволяет проверить корректность алгоритма поиска компонент связности и производительность графа.

### Инструкция по запуску:

1.  **Генерация данных:**
    Убедитесь, что в папке есть файл `dictionary.txt` (список слов для названий хостов) и запустите скрипт генерации:
    ```bash
    python3 generator.py
    ```
    *Это создаст файл `input.txt` с тысячами вершин и настроенными связями внутри подсетей.*

2.  **Сборка специальной версии:**
    Соберите проект с флагом `ADDITIONAL`, который переключает программу в режим автоматической обработки файла:
    ```bash
    make build_asan_addit
    ```

3.  **Запуск анализа:**
    ```bash
    ./bin_asan
    ```

### Результат работы
Программа автоматически:
1.  Считает огромный граф из `input.txt`.
2.  Выполнит алгоритмическое разбиение графа на компоненты связности (подсети).
3.  Сохранит результаты (списки компьютеров по подсетям) в файл `additional_task_result.txt`.
4.  Освободит всю выделенную память (контролируется ASan)
5.  Файл dictionary.txt не предусмотрен, вы можете использовать свой.